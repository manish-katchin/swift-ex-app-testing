# Disable parallel execution to ensure proper driver cleanup between scenarios
junit.jupiter.execution.parallel.enabled = false
junit.jupiter.execution.parallel.mode.default = same_thread
junit.jupiter.execution.parallel.config.strategy = dynamic
# Cucumber plugin settings can be passed via system properties; Allure will pick up results
