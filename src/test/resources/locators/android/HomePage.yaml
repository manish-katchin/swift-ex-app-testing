# Balance Section
balance_amount:
  strategy: androiduiautomator
  value: new UiSelector().className("android.widget.TextView").textMatches("\\$ [0-9]+\\.[0-9]+")

balance_currency:
  strategy: androiduiautomator  
  value: new UiSelector().className("android.widget.TextView").text("X.XX")

# Action Buttons
receive_button:
  strategy: xpath
  value: //*[contains(@text,'Receive')]

send_button:
  strategy: androiduiautomator
  value: new UiSelector().text("Send")

swap_button:
  strategy: androiduiautomator
  value: new UiSelector().text("Swap")

buy_button:
  strategy: androiduiautomator
  value: new UiSelector().text("Buy")

# Navigation Bar
home_tab:
  strategy: androiduiautomator
  value: new UiSelector().text("Home")

wallet_tab:
  strategy: androiduiautomator
  value: new UiSelector().text("Wallet")

market_tab:
  strategy: androiduiautomator
  value: new UiSelector().text("Market")

exchange_tab:
  strategy: androiduiautomator
  value: new UiSelector().text("Exchange")

settings_tab:
  strategy: androiduiautomator
  value: new UiSelector().text("Settings")

# Asset Action Buttons (context-dependent)
trade_button:
  strategy: androiduiautomator
  value: new UiSelector().text("Trade")

asset_buy_button:
  strategy: androiduiautomator
  value: new UiSelector().text("Buy")

# Exchange Page Locators
trade_wallet_header:
  strategy: androiduiautomator
  value: new UiSelector().text("Trade Wallet")

home_icon:
  strategy: androiduiautomator
  value: new UiSelector().text("Home")

offers_icon:
  strategy: androiduiautomator
  value: new UiSelector().text("Offers")

transactions_icon:
  strategy: androiduiautomator
  value: new UiSelector().text("Transactions")

on_off_ramp_icon:
  strategy: androiduiautomator
  value: new UiSelector().text("On/Off Ramp")

profile_icon:
  strategy: androiduiautomator
  value: new UiSelector().text("Profile")

# Settings Page Locators
settings_header:
  strategy: androiduiautomator
  value: new UiSelector().text("Settings")

choose_wallet_option:
  strategy: androiduiautomator
  value: new UiSelector().text("Choose Wallet")

dark_mode_option:
  strategy: androiduiautomator
  value: new UiSelector().text("Dark Mode")

exchange_option:
  strategy: androiduiautomator
  value: new UiSelector().text("Exchange")

transactions_option:
  strategy: androiduiautomator
  value: new UiSelector().text("Transactions")

biometric_authentication_option:
  strategy: androiduiautomator
  value: new UiSelector().text("Biometric Authenticaton")

preference_option:
  strategy: androiduiautomator
  value: new UiSelector().text("Preference")

push_notification_option:
  strategy: androiduiautomator
  value: new UiSelector().text("Push Notification")

help_center_option:
  strategy: androiduiautomator
  value: new UiSelector().text("Help Center")

logout_option:
  strategy: xpath
  value: //android.view.ViewGroup[contains(@content-desc="Log Out")]

back_button:
  strategy: androiduiautomator
  value: new UiSelector().description("Navigate up")

TradeWallet_back_button:
  strategy: accessibility
  value: trade_wallet_back

# Locator for homepage

ethereum:
  strategy: xpath
  value: //android.view.ViewGroup[contains(@content-desc,'Ethereum')]

xlm:
  strategy: xpath
  value: //android.view.ViewGroup[contains(@content-desc,'XLM')]

binance:
  strategy: xpath
  value: //android.view.ViewGroup[contains(@content-desc,'Binance')]

bitcoin:
  strategy: xpath
  value: //android.view.ViewGroup[contains(@content-desc,'Bitcoin')]


xlm_trade_button:
  strategy: xpath
  value: //*[contains(@content-desc,'XLM')]/parent::*//android.widget.TextView[@text='Trade']

xlm_buy_button:
  strategy: xpath
  value: //*[contains(@content-desc,'XLM')]/parent::*//android.widget.TextView[@text='Buy']

binance_trade_button:
  strategy: xpath
  value: //*[contains(@content-desc,'Binance')]/parent::*//android.widget.TextView[@text='Trade']

binance_buy_button:
  strategy: xpath
  value: //*[contains(@content-desc,'Binance')]/parent::*//android.widget.TextView[@text='Buy']

bitcoin_arrivingsoon_button:
  strategy: xpath
  value: //*[contains(@content-desc,'Bitcoin')]/parent::android.view.ViewGroup//*[@text='Arriving soon']

ethereum_trade_button:
  strategy: xpath
  value: //*[contains(@content-desc,'Ethereum')]/parent::*//android.widget.TextView[@text='Trade']

ethereum_buy_button:
  strategy: xpath
  value: //*[contains(@content-desc,'Ethereum')]/parent::*//android.widget.TextView[@text='Buy']

# Asset Tab Section
assets_tab:
  strategy: androiduiautomator
  value: new UiSelector().text("Assets")

add_assets_tab:
  strategy: androiduiautomator
  value: new UiSelector().text("Add Assets")

# Asset List
bitcoin_asset:
  strategy: androiduiautomator
  value: new UiSelector().text("Bitcoin")

usdc_asset:
  strategy: androiduiautomator
  value: new UiSelector().text("USDC")

usdt_asset:
  strategy: androiduiautomator
  value: new UiSelector().text("USDT")

# Buy Page header
buy_page_header:
  strategy: xpath
  value: //*[@text="Deposit/Withdrawal"]

eyeball_icon:
  strategy: xpath
  value: //android.view.ViewGroup[@resource-id='eye ball icon']

wallet_balance_hidden:  
  strategy: xpath
  value: //android.widget.TextView[@text="$ X.XX "]

wallet_balance_visible:
  strategy: xpath
  value: //android.widget.TextView[contains(@text, "$ 0")]

dropdown_icon:
  strategy: xpath
  value: //android.view.ViewGroup[@resource-id="drop down"]

choose_wallet_header:
  strategy: xpath
  value: //android.widget.TextView[@text="Choose wallet"] 

Add_Wallet_button:
  strategy: xpath
  value: //*[@content-desc="Add Wallet"]

all_wallets_in_dropdown:
  strategy: xpath
  value: //android.widget.ScrollView/android.view.ViewGroup/android.view.ViewGroup[android.widget.TextView]